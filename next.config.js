/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.azapfy.com.br",
      "www.jsdelivr.com",
      "www.cdn.jsdelivr.net",
      "cdn.jsdelivr.net",
    ],
  },
};

module.exports = nextConfig;
